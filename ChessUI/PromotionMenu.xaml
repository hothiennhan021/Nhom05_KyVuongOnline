<UserControl x:Class="ChessUI.PromotionMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChessUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="PromotionBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#FF252525"/>
            <Setter Property="BorderBrush" Value="#FFFEFEFE"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.8"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PromotionTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#FFE1C699"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Border Width="275" Height="125"
            Style="{StaticResource PromotionBorderStyle}">

        <Grid RenderOptions.BitmapScalingMode="HighQuality">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="1.5*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="CHỌN 1 CON CỜ"
                       Grid.Row="0"
                       Style="{StaticResource PromotionTextStyle}"
                       FontSize="24"/>

            <UniformGrid Grid.Row="1"
                         Rows="1"
                         Columns="4"
                         Cursor="Hand">

                <Image x:Name="QueenImg"
                       MouseDown="QueenImg_MouseDown"
                       Source="Assets/QueenW.png"
                       Margin="5"/>

                <Image x:Name="BishopImg"
                       MouseDown="BishopImg_MouseDown"
                       Source="Assets/BishopW.png"
                       Margin="5"/>

                <Image x:Name="RookImg"
                       MouseDown="RookImg_MouseDown"
                       Source="Assets/RookW.png"
                       Margin="5"/>

                <Image x:Name="KnightImg"
                       MouseDown="KnightImg_MouseDown"
                       Source="Assets/KnightW.png"
                       Margin="5"/>
            </UniformGrid>
        </Grid>
    </Border>
</UserControl>